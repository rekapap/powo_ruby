<section class="card">
  <h1 class="h1">Basic views + routes</h1>
  <p class="muted">
    This is a tiny Sinatra app that uses <code>powo_ruby</code> via a local <code>path:</code> Gemfile entry.
    Its dependencies live under <code>examples/basic_app</code> and are excluded from the published gem.
  </p>
</section>

<section class="card">
  <h2 class="h2">Search</h2>
  <form method="get" action="/search" class="form">
    <div class="grid">
      <label class="field">
        <span class="field__label">Query (required)</span>
        <input class="field__input" type="text" name="q" value="Acacia" />
      </label>

      <label class="field">
        <span class="field__label">Per page</span>
        <input class="field__input" type="number" name="per_page" value="24" min="1" max="500" />
      </label>

      <label class="field">
        <span class="field__label">Family (optional)</span>
        <input class="field__input" type="text" name="family" placeholder="Fabaceae" />
      </label>

      <label class="field">
        <span class="field__label">Genus (optional)</span>
        <input class="field__input" type="text" name="genus" placeholder="Acacia" />
      </label>

      <label class="field">
        <span class="field__label">Accepted (optional)</span>
        <select class="field__input" name="accepted">
          <option value="" selected>(any)</option>
          <option value="true">true</option>
          <option value="false">false</option>
        </select>
      </label>
    </div>

    <div class="actions">
      <button class="btn" type="submit">Search</button>
      <a class="btn btn--secondary" href="/taxon?id=urn:lsid:ipni.org:names:30000618-2">Try lookup</a>
    </div>
  </form>
</section>

